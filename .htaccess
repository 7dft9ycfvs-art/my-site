RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]

# Защита от доступа к конфигурационным файлам
<FilesMatch "\.(env|json|config|lock|gitignore)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
